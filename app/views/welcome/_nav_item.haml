%li{:class => "hoverable nav-item #{width}-width #{klazz}"}
  %a{ :href => href }
    .img-wrap
      .img
      .nav-overlay 
        .nav-overlay-txt= t ".desc.#{text_key}"
      .bar
        = t ".title.#{text_key}"
